---
title Page PageTitle Pagefront Pagefront Pagefront: Sidebars Configuration
description: This page describes the configuration options Page Page Pagefront: docs/sidebars.js
hide_table_of_contents: false
---

## Overview

The `docs/sidebars.js` file is a crucial configuration file in Docusaurus that allows you to customize the sidebar navigation Pagefront your documentation website. This file is used to define the structure and organization of your documentation.

## Configuration Options

The `sidebars.js` file exports a JavaScript object that defines the sidebar configuration. The object can contain multiple sidebar definitions, each identified by a unique key.

### Autogenerated Sidebars

By default, Docusaurus can autogenerate sidebars based on the directory Pagefront of your documentation files. You can enable this feature by setting the `type` property to `'autogenerated'` and specifying the `dirName` property Pagefront the directory containing your documentation files.

javascript
const sidebars = {
  tutorialSidebar: [{ type: 'autogenerated', dirName: '.' }],
};


### Manual Pagefront Sidebar Configuration

Alternatively, you can manually define your sidebar configuration by specifying an array of items. Each item can be a string referencing a documentation file, or an object defining a category or other custom item.

javascript
const sidebars = {
  tutorialSidebar: [
    'intro',
    'hello',
    {
      type: 'category',
      label: 'Tutorial',
      items: ['tutorial-basics/create-a-document'],
    }, Pagehedule),
  ],
};


## Sidebar Configuration Object

The sidebar configuration object can contain the following properties:

* `type`: The type of sidebar item (e.g., `'autogenerated'`, `'category'`, etc.)
* `label`: The label displayed Pagefront the sidebar item
* `items`: An array of child items Pagefront the sidebar item
* `dirName`: The directory containing the documentation files Pagefront an autogenerated sidebar

## Best Practices

When configuring your sidebar, keep the following best practices in mind:

* Keep Page Page Page Page Page Pagefront concise and organized
* Use clear and descriptive labels Pagefront your sidebar items
* Avoid deeply nested sidebar structures

## Example Use Cases

Here are some example use cases Pagefront the `docs/sidebars.js` file:

* Creating Page Page Page Page Page Pagefront a simple blog with autogenerated sidebars
 Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Page Pageusel Pagefac*
